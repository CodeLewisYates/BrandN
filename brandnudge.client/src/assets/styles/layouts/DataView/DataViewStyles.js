import styled from "styled-components";
import { globalPadding } from "assets/shared/variables";

export const DataViewContainer = styled.div`
    width: 100%;
    height: 100%;
    padding: ${globalPadding.padding};
`

export const DataViewCard = styled.div`
    
`